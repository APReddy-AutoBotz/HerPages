{
  "name": "@herpages/vault-port",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc",
    "test": "echo 'vault-port: interfaces only — no runtime tests until HP-003'",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@herpages/contracts": "workspace:*"
  },
  "devDependencies": {
    "typescript": "7.0.2"
  }
}
